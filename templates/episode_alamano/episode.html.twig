{% extends 'base.html.twig' %}

{% block title %}Saison {{ season.number }} : Episode {{ season.number }}{% endblock %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }} : saison {{ season.number }}</h1>
            <h2>Episode {{ episode.number }} : {{ episode.title }}</h2>
            <p>Synopsis : {{ episode.synopsis }}</p>
        </div>
    </div>

    <section>
    <h2>Tous les épisodes de la saison {{ season.number }}</h2>

    {% for episode in season.episodes %}
    <p>
        <a href="{{ path("program_episode_show", { program: program.id , season: season.id, episode: episode.id}) }}"><h3>Episode {{ episode.number }} : {{ episode.title }}</h3></a>
    </p>
    {% endfor %}
    </section>

    <div>

        <p>
            <a href="{{ path("program_show", { id: program.id }) }}"> Retour vers {{ program.title }}</a>
        </p>

    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
{% endblock %}