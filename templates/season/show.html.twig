{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
            
        </tbody>
    </table>

    <table class="table">
        <tbody>
        <h2>Liste des épisodes disponibles</h2>
            <tr>
                {% for episode in episodes %}
                <tr>
                    <div>
                    <a href="{{ path('app_episode_show', {'id': episode.id}) }}">{{ loop.index }} - {{ episode.title }}</a>
                    </div>
                </tr>
                {% endfor %}
            </tr>
         </tbody>
    </table>

    <a href="{{ path('app_season_index') }}">back to list</a>

    <a href="{{ path('app_season_edit', {'id': season.id}) }}">edit</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
